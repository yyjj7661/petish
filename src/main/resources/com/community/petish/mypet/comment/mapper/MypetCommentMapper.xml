<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.community.petish.mypet.comment.mapper.MypetCommentMapper">

	<insert id="save">
		<selectKey keyProperty="commentId" resultType="Long" order="BEFORE">
			SELECT mypet_comment_seq.nextval from dual
		</selectKey>
	
		INSERT INTO mypet_comment_tb
			(id, content, post_id, user_id)
		VALUES
			( #{commentId}, #{content}, #{postId}, #{userId})
	</insert>



</mapper>