<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.community.petish.mypage.mapper.UserMapper">

	<select id="findUser" parameterType="long" resultType="com.community.petish.user.dto.UserResponseDTO">
		select id, username, nickname, picture, deleted, address
		from user_tb
		where id =#{id}
	</select> 
	
	<update id="modifyUserInfo" parameterType="com.community.petish.user.dto.UserModifyRequestDTO">
		update user_tb set nickname=#{nickname},address=#{address}, password=#{password}
		where id = #{id}
	</update>

	<update id="modifyPicture" parameterType="com.community.petish.user.dto.UserModifyPictureDTO">
		update user_tb set picture=#{picture}
		where id = #{id}
	</update>
	
</mapper>